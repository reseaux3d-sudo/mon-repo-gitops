# nginx-ubi.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-demo
  template:
    metadata:
      labels:
        app: nginx-demo
    spec:
      containers:
      - name: nginx
        image: ghcr.io/reseaux3d-sudo/nginx-demo-repo:3b05d5793d74b88004a34bbdbb50189ab16ae5e4
        command: ["nginx", "-g", "daemon off;"]   # commande explicite
        ports:
        - containerPort: 8080
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          capabilities:
            drop: ["ALL"]
